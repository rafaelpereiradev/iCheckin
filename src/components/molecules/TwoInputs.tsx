import {Box,TextField,Text} from '../'
import { TwoInputs } from '../types/icheck-types'


export default function TwoInputs({flexDirection}:TwoInputs) {
  return (
    <Box flexDirection={flexDirection}>

    </Box>
  )
}
